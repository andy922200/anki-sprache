{
  "name": "anki-sprache",
  "version": "0.1.0",
  "private": true,
  "description": "Anki-style multi-language learning web app with FSRS spaced repetition",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:app": "pnpm --filter app dev",
    "dev:server": "pnpm --filter server dev",
    "dev:worker": "pnpm --filter server dev:worker",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "db:migrate": "pnpm --filter server prisma migrate dev",
    "db:seed": "pnpm --filter server prisma db seed",
    "db:studio": "pnpm --filter server prisma studio",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  }
}
